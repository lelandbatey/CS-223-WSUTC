#include "node_template.cpp"

class lbtree{
    node_template<int>* root;
    void addNode(int, node_template<int>*);

public:
    // tree(arguments);
    // ~tree();

    void add(int);

    /* data */
};
